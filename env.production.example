# =============================================
#  Outreach — Production Environment Variables
# =============================================
#
#  На сервере скопируйте этот файл как .env:
#    cp env.production.example .env
#
# =============================================

# ── PostgreSQL ──
POSTGRES_DB=outreach
POSTGRES_USER=outreach_user
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# ── Backend ──
JWT_SECRET=CHANGE_ME_RANDOM_STRING_64_CHARS
JWT_EXPIRES_IN=30d

# ── Dashboard ──
# URL API для фронтенда. Через Nginx на одном сервере — используйте /api
# Chrome Extension подключается к http://YOUR_DROPLET_IP/api
NEXT_PUBLIC_API_URL=/api
